services:
  api:
    container_name: capstone
    image: capstone_drintim
    build: .
    ports:
      - 3000:3000
    environment:
      - DB_USER=leome
      - DB_PASSWORD=2909
      - DB_HOST=db
      - DB_PORT=5432
      - DB=capstone_teste
    depends_on:
      - db
  db:
    container_name: db_capstone
    image: postgres:12
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=leome
      - POSTGRES_PASSWORD=2909
      - POSTGRES_DB=capstone_teste
    volumes:
      - ./create_tables.sql:/docker-entrypoint-initdb.d/create_tables.sql
      - ./backend/:/code/src/app
      - /code/src/app/node_modules
# onde constar "atualizar, substituir pelos padrões adotados para o projeto"